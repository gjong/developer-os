# Live image: land directly in Plasma (Wayland via /usr/share/wayland-sessions/plasma.desktop).
# Disk installs use SDDM without this file (fresh pacstrap).
[Autologin]
User=liveuser
Session=plasma.desktop
