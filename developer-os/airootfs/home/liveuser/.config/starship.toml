# Minimal Starship — adjust to taste
format = "$username$hostname$directory$git_branch$character"

[character]
success_symbol = "[❯](bold green) "
error_symbol = "[❯](bold red) "

[directory]
truncation_length = 4
truncate_to_repo = true
